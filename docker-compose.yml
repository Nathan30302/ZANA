services:
  db:
    image: postgres:16-alpine
    container_name: zana-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: zana
    volumes:
      - zana_pg:/var/lib/postgresql/data

volumes:
  zana_pg:
