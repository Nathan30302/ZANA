# ZANA Pro

Flutter provider app — online toggle, float balance, accept jobs.

```bash
export PATH="$HOME/development/flutter/bin:$PATH"
flutter create --platforms=ios,android .
flutter pub get
flutter run --dart-define=API_URL=http://localhost:3000/v1
```

Dev login uses seeded provider `+260970000001` / OTP `123456`.
